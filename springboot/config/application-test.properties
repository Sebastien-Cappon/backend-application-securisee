# Global configuration
spring.application.name=poseidon

# Lazy Spring's Open Session In View (OSIV) configuration. 
spring.jpa.open-in-view=true

# Tomcat configuration
server.port=9090

# Database configuration
spring.datasource.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE
spring.datasource.username=H2Sebastien
spring.datasource.password=h2Dat@T357
spring.datasource.initialize=true

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=false

# Logging configuration
logging.level.root=ERROR
logging.level.org.springframework=INFO
logging.level.org.springframework.boot.web.embedded.tomcat=INFO